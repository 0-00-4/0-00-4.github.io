A Pen created at CodePen.io. You can find this one at http://codepen.io/MattCowley/pen/jqBbdG.

 Console site, uses an object to store all commands, can pass data to them. All runs JS if it is directly inputted into the site command line.

Seems to be an issue in safari where the backspace deletes the entire input, I'm not sure why.